{
  "name": "@cx402/agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "cx402 Agent SDK — give an AI agent a compliance mandate; it pays only when the payment is verified and within policy.",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "viem": "^2.52.2"
  },
  "devDependencies": {
    "@cx402/facilitator": "workspace:*"
  }
}
